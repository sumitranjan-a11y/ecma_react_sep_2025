import ErrorHandler from '../common/ErrorHandler';

const RootComponent = () => {
    return (
        <ErrorHandler>
            <div className='container'>
            </div>
        </ErrorHandler>
    );
};

export default RootComponent;